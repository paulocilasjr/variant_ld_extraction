# Data

- `inputs/`: curated project inputs used by scripts. These are the files you edit when changing SNPs, regions, or provenance.
- `public/`: downloaded third-party/public datasets. Keep accession-specific files here so generated outputs can cite their source.

Generated analysis products should go under `outputs/`, not `data/`.
